import { Typography } from '../../components/ui';

/** Placeholder — o conteúdo real do Dashboard chega na Etapa 4. */
export function DashboardPage() {
  return (
    <div className="flex h-full min-h-[60vh] items-center justify-center rounded-xl border border-dashed border-gray-300 dark:border-gray-700">
      <Typography variant="body">Conteúdo do Dashboard chega na Etapa 4.</Typography>
    </div>
  );
}
